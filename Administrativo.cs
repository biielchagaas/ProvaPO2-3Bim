namespace ProvaPO2_3Bim
{
    internal class Administrativo : Funcionario
    {
        public Administrativo(string nome, string cpf, int matricula, DateTime dataNasc, string sexo, double salario) : base
            (nome, cpf, matricula, dataNasc, sexo, salario)
        { 

        }
    }
}
